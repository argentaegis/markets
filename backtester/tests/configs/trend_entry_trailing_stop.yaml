# Test config — trend_entry_trailing_stop with trailing stop, uses fixtures
symbol: ESH1
broker: zero
initial_cash: 500000.0  # ESH1 ~5412 needs ~270k per contract
start: "2026-01-02T14:30:00+00:00"
end: "2026-01-02T16:00:00+00:00"
timeframe_base: "1m"
instrument_type: future
futures_contract_spec:
  symbol: ESH1
  tick_size: 0.25
  point_value: 50.0
  session:
    name: RTH
    start_time: "09:30:00"
    end_time: "16:00:00"
    timezone: America/New_York

strategy:
  name: trend_entry_trailing_stop
  params:
    qty: 1
    ma_period: 1  # 2 bars needed for fixture (1m bars available)
    trailing_stop_ticks: 4
    direction: LONG

data_provider:
  underlying_path: src/loader/tests/fixtures/underlying_trailing  # ESH1_1m
  options_path: src/loader/tests/fixtures/options
  timeframes_supported: ["1d", "1h", "1m"]
  missing_data_policy: RETURN_PARTIAL
  max_quote_age: null
