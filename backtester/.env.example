# Market data — Massive (required for fetch). POLYGON_API_KEY also supported.
MASSIVE_API_KEY=

# Polygon free tier: 5 req/min. Set to 1/true/yes to enable rate-limit waits (65s initial + 15s between quotes).
# Default: off (no throttling).
# MASSIVE_RATE_LIMIT=
